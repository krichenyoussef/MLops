apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fraud-api
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fraud-api
                port:
                  number: 80
